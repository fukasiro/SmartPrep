
services:
  backend:
    build:
      context: ./backend
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./front
    ports:
      - "5173:4173"
    depends_on:
      - backend
